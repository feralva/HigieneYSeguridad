import { HasPatenteDirective } from './has-patente.directive';

describe('HasPatenteDirective', () => {
  it('should create an instance', () => {
    const directive = new HasPatenteDirective();
    expect(directive).toBeTruthy();
  });
});
