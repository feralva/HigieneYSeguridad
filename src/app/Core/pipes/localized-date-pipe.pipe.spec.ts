import { LocalizedDatePipePipe } from './localized-date-pipe.pipe';

describe('LocalizedDatePipePipe', () => {
  it('create an instance', () => {
    const pipe = new LocalizedDatePipePipe();
    expect(pipe).toBeTruthy();
  });
});
